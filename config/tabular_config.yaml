thyroid:
  batch_size: 32
  lr: 0.0001
  subset_size: 3
  overlap: 0
  hidden_dim: 512
  normalization: minmax

wine:
  batch_size: 8
  lr: 0.005
  subset_size: 5
  overlap: 1
  hidden_dim: 32
  normalization: minmax

vertebral:
  batch_size: 16
  lr: 0.001
  subset_size: 3
  overlap: 0
  hidden_dim: 512
  normalization: minmax